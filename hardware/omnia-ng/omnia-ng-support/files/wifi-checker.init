#!/bin/sh
if uci show wireless | grep -q VHT[0-9]; then
    create_notification -s error 'Your Wi-Fi is set to Wi-Fi 5. Wi-Fi 7 should be stable now again, you can try switching back to Wi-Fi 7 (802.11be).'
fi
